export type PatientCaseDetail = {
  case_id: string; // text
  tag: string; // character varying
  created_at: string; // timestamp with time zone
  patient_id: string;
  access_uid: string; // text
};
